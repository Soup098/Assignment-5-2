#include <iostream>

using namespace std;

// create function to open FahrenheitTemperature.txt

// create function that reads the the contents of that file as the input

// create function that takes the tempurature readings from that file and converts them into celcius

// create a new file called CelciusTemperature.txt and writes the converted temperature to the file.

// close the file.

// main function to run said functions.